spring:
  datasource:
    url: jdbc:postgresql://localhost:5437/rza_mpei
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        hbm2ddl:
          auto: create